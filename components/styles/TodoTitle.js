import { StyleSheet } from 'react-native'
import { FONT, FONTWEIGHT } from '../../themes/index'
export const styleTitle = StyleSheet.create({
  title: {
    fontSize: FONT.XL,
    fontWeight: FONTWEIGHT.BOLD,
    marginBottom: 35,
    marginTop: 10
  }
})
